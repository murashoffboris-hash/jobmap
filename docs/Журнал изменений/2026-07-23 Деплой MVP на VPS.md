# Изменение: Деплой MVP на VPS 104.237.11.110

Дата: 2026-07-23 08:15
Git-ветка: main (локально: infra/initial-audit)
Git commit: локально edbdf75, сервер /opt/jobmap

## Что изменено

Развёрнут JobMap MVP на VPS 104.237.11.110 (Ubuntu 24.04).

## Зачем это сделано

Первый рабочий деплой для проверки инфраструктуры.

## Какие сервисы запущены

| Сервис | Статус | Порт |
|--------|--------|------|
| jobmap-postgres (PostGIS 16-3.4) | ✅ healthy | 127.0.0.1:5432 |
| jobmap-backend (FastAPI) | ✅ healthy | 127.0.0.1:8001 |
| jobmap-frontend (nginx:alpine) | 🟡 запускается | 127.0.0.1:3000 |
| jobmap-nginx (reverse proxy) | 🟡 запускается | 0.0.0.0:80, :443 |
| jobmap-redis | ✅ healthy | 127.0.0.1:6379 |
| jobmap-worker (Celery) | 🟡 unhealthy | internal |
| jobmap-minio (S3) | ✅ healthy | 127.0.0.1:9000, :9001 |

## Результаты проверки

- ✅ curl http://localhost:80/ — отдаёт index.html
- ✅ curl http://localhost:80/health — отдаёт JSON health
- ✅ Backend отвечает, healthcheck работает
- ⚠️ Worker unhealthy — Celery не может подключиться к Redis (требует диагностики)
- ⚠️ Зависимости (postgresql, redis, nominatim, osrm) показывают "pending" — реальная проверка зависимостей не реализована в health endpoint

## Переменные окружения

`/opt/jobmap/.env` — POSTGRES_PASSWORD, JWT_SECRET изменены от дефолтных.

## Миграции базы данных

Не запущены — Alembic требует подключения к БД.

## Следующие шаги

1. Запустить Alembic миграции (создать таблицы)
2. Исправить Celery worker
3. Реализовать реальную проверку зависимостей в /health
4. Добавить Nominatim и OSRM (пока placeholder)
5. Реализовать JWT auth
