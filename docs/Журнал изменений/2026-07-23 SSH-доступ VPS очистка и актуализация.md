# 2026-07-23 — SSH-доступ к VPS: очистка и актуализация

## Что сделано

### Документация доступа

1. **Создан `docs/Развертывание/Реквизиты_доступа_VPS.md`** — единое место с описанием SSH-ключа и где брать пароль (внешний секрет-менеджер). В `.gitignore`.
2. **Обновлён `docs/Развертывание/VPS 104.237.11.110.md`** — убран пароль root, убран устаревший путь к ключу `vps_phone`. Добавлена секция «Известные проблемы» с текущим статусом контейнеров.
3. **`/d/Obsidian/Asterisk/01_Сервер_и_ОС.md`** — добавлено предупреждение: локальный Hyper-V (192.168.1.9) ≠ VPS 104.237.11.110.

### Безопасность

4. **`chmod 600` на `~/.ssh/vps_service247`** — права доступа были `644`, что запрещало SSH использовать ключ.

### `.gitignore`

5. Добавлено правило `**/deploy/ssh/*` и `docs/**/Реквизиты_доступа*.md` — чтобы случайно не закоммитить ключи/пароли.

## Найдено в ходе проверки

- Ключ `vps_service247` уже лежит в `~/.ssh/`, пароль 644 → починено → `chmod 600`.
- VPS доступен по SSH через ключ `vps_service247`.
- 7 контейнеров JobMap работают, 3 показывают `unhealthy` (но реально работают — просто нет healthcheck).
- Backend `/health` возвращает пусто при curl из shell-сессии — нужно локально проверить запуском `uvicorn`.

## Что НЕ сделано

- Не настроен Let's Encrypt — HTTP без редиректа на HTTPS.
- Не добавлены healthcheck для `jobmap-nginx`, `jobmap-frontend`, `jobmap-worker`.
- Не выяснена причина пустого ответа `curl /health` на backend.

## Команды для будущих сессий

```bash
# Подключение к VPS
ssh -i ~/.ssh/vps_service247 root@104.237.11.110
```
