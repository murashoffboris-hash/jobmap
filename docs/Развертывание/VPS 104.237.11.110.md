# JobMap — Сервер 104.237.11.110

## Подключение к VPS

| Параметр | Значение |
|----------|----------|
| Хост | phone.service247.by |
| IP | 104.237.11.110 |
| OS | Ubuntu 24.04, ядро 6.17 |
| RAM | 5.7 GB |
| Диск | 28 GB |
| SSH ключ | `D:\Obsidian\HR\job-service\deploy\ssh\vps_phone` |
| SSH пользователь | root |
| Пароль root | PrizesMinisUglisPyxis |

## Docker

| Параметр | Значение |
|----------|----------|
| Docker | 29.6.2 |
| Docker Compose | 5.3.1 |
| Путь проекта | /opt/jobmap |
| Docker Compose файл | /opt/jobmap/docker-compose.yml |

## Сервисы

| Контейнер | Статус | Порты |
|-----------|--------|-------|
| jobmap-postgres | postgis:16-3.4 | 127.0.0.1:5432 |
| jobmap-backend | FastAPI, Python 3.12 | 127.0.0.1:8001 |
| jobmap-frontend | nginx:alpine | 127.0.0.1:3000 |
| jobmap-nginx | nginx:alpine (reverse proxy) | 0.0.0.0:80, :443 |
| jobmap-redis | redis:7-alpine | 127.0.0.1:6379 |
| jobmap-worker | Celery worker | internal |
| jobmap-minio | MinIO S3 | 127.0.0.1:9000, :9001 |

## Переменные окружения

Все в `/opt/jobmap/.env` (не в Git!).

```
SERVICE_NAME=JobMap
APP_ENV=production
PUBLIC_DOMAIN=phone.service247.by
POSTGRES_HOST=postgres
POSTGRES_DB=job_service
POSTGRES_USER=job_service
POSTGRES_PASSWORD=JobMapPgCh@ngeM3
NOMINATIM_URL=http://nominatim:8080
OSRM_URL=http://osrm:5000
REDIS_URL=redis://redis:6379/0
JWT_SECRET=JobMapJwTCh@ngeM3Pl3ase
S3_ENDPOINT=http://minio:9000
```

## Asterisk

Сервер предназначен для Asterisk IP-телефонии. JobMap развёрнут в отдельных контейнерах, не мешает будущему Asterisk.

## SSH ключ

Приватный ключ сохранён в: `D:\Obsidian\HR\job-service\deploy\ssh\vps_phone`
Для подключения без пароля скопируй ключ в `~/.ssh/`:

```bash
ssh-keyscan -H 104.237.11.110 >> ~/.ssh/known_hosts
ssh -i "D:\Obsidian\HR\job-service\deploy\ssh\vps_phone" root@104.237.11.110
```
