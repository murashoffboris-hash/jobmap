# 06 Журнал изменений

Сводный журнал изменений проекта JobMap. Подробности — в `docs/Журнал изменений/`.

## 2026-07-24 — Сессия: фиксы, вакансии, карта, профиль
- Fix: UserRole и VacancyStatus — `values_callable` для правильной сериализации enum в БД
- Fix: frontend baseURL `/api/v1` → `/api` (совпадает с бэкендом)
- Fix: Nominatim/OSRM убраны из health-check → статус `ok`
- Fix: `GET /api/vacancies` — пагинация, eager loading (selectinload), lat/lon опциональны
- Fix: `serialization_alias` `currency` для VacancyListItem
- Fix: CSS для HomePage — карта `.map-container` и `.layout` получили размеры
- Fix: стиль карты — CartoDB Positron вместо demotiles (города, улицы)
- Add: Vitest для frontend — Button, Input, Avatar (14 тестов passed)
- Add: ADR-001…004, журнал изменений, чеклист Review, AGENTS.md
- Add: тестовая вакансия «Разработчик Python 1500 BYN» и профиль «Иван Петров»
- Add: страница профиля `/profile` с редактированием (feature/profile-page)
- Add: backend `PATCH /api/auth/me`, расширенный `GET /api/auth/me` 
- Deploy: недостающие файлы доставлены на VPS
- Kanban: созданы таски на профиль пользователя (Frontend + Backend)
- [Подробнее](docs/Журнал%20изменений/2026-07-24%20Сессия%20—%20фиксы%20вакансии%20карта%20профиль.md)

## 2026-07-24 — Рефакторинг health-роутера, Celery, зависимости, тесты
- Рефакторинг health endpoint: вынесен в отдельный роутер, Celery health через ping задачи
- Фикс цепочки fallback-моделей для профилей
- Обновлён VPS.md с актуальными данными
- 18 тестов passed, 4 skipped
- [Подробнее](docs/Журнал изменений/2026-07-24%20Рефакторинг%20health%20роутер%20Celery%20зависимости%20тесты.md)

## 2026-07-23 — Завершение MVP
- Backend MVP: auth (JWT), CRUD вакансий, geo-эндпоинты, 18 тестов
- База данных: 15+ таблиц, PostGIS индексы, сидинг, Alembic миграции, pg_dump backup
- Инфраструктура: SSL/HTTPS (Let's Encrypt), healthcheck для всех контейнеров, мониторинг, backup cron
- Frontend: базовая архитектура (React + TS + Vite + Capacitor + PWA + MapLibre)
- Frontend: редизайн auth-страниц (Tailwind + dark mode + glassmorphism + framer-motion)
- Frontend: Header с адаптивом, ProtectedRoute, UI-примитивы
- Favicon, nginx.conf подготовка к HTTPS
- [Сводка](docs/Журнал изменений/2026-07-23%20Сводка%20—%20все%20завершённые%20задачи.md)
